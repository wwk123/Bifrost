# Polkadot 黑客松 - 技术资源与最佳实践

> 全面的技术参考资料、开发工具、代码模板和最佳实践指南

---

## 📚 目录

1. [开发环境搭建](#开发环境搭建)
2. [核心技术文档](#核心技术文档)
3. [代码模板库](#代码模板库)
4. [测试网资源](#测试网资源)
5. [UI/UX 设计资源](#ui-ux-设计资源)
6. [部署与DevOps](#部署与devops)
7. [常见问题解决](#常见问题解决)

---

## 🛠️ 开发环境搭建

### 1.1 完整开发环境安装指南

#### **Step 1: 基础工具安装**

```bash
# 1. 安装 Node.js (v18+)
# 下载: https://nodejs.org/
node --version  # 验证: v18.0.0+

# 2. 安装 Rust (赛道一 REVM/PVM 开发需要)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
rustc --version  # 验证

# 3. 安装 Git
git --version

# 4. 安装 VS Code
# 下载: https://code.visualstudio.com/
```

#### **Step 2: Polkadot 开发工具**

```bash
# 安装 Polkadot.js CLI
npm install -g @polkadot/api-cli

# 安装 Substrate 合约工具 (可选)
cargo install cargo-contract --force

# 验证安装
polkadot-js-api --version
```

#### **Step 3: 赛道一特定工具**

```bash
# 安装 Foundry (Solidity 开发)
curl -L https://foundry.paradigm.xyz | bash
foundryup

# 验证
forge --version
cast --version

# 安装 Hardhat (备选)
npm install --global hardhat
```

#### **Step 4: 赛道二特定工具**

```bash
# 安装 Bifrost SDK
npm install @bifrost-finance/sdk

# 安装 Polkadot.js 扩展
# Chrome: https://chrome.google.com/webstore/detail/polkadot%7Bjs%7D-extension/
```

---

### 1.2 VS Code 推荐配置

**必装插件**:

```json
{
  "recommendations": [
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "ms-vscode.vscode-typescript-next",
    "bradlc.vscode-tailwindcss",
    "JuanBlanco.solidity",
    "rust-lang.rust-analyzer",
    "ms-azuretools.vscode-docker"
  ]
}
```

**settings.json 配置**:

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "[solidity]": {
    "editor.defaultFormatter": "JuanBlanco.solidity"
  },
  "[rust]": {
    "editor.defaultFormatter": "rust-lang.rust-analyzer"
  }
}
```

---

## 📖 核心技术文档

### 2.1 赛道一文档

#### **Polkadot 官方文档**

| 文档 | 链接 | 说明 |
|------|------|------|
| Polkadot Wiki | https://wiki.polkadot.network/ | 全面的 Polkadot 知识库 |
| Substrate Docs | https://docs.substrate.io/ | Substrate 开发文档 |
| Polkadot.js Docs | https://polkadot.js.org/docs/ | JS API 完整文档 |
| XCM Format | https://github.com/paritytech/xcm-format | 跨链消息格式规范 |

#### **REVM 相关**

| 资源 | 链接 | 说明 |
|------|------|------|
| REVM GitHub | https://github.com/bluealloy/revm | REVM 核心代码库 |
| EVM 规范 | https://ethereum.github.io/execution-specs/ | 以太坊虚拟机规范 |
| Foundry Book | https://book.getfoundry.sh/ | Foundry 完整教程 |
| Solidity Docs | https://docs.soliditylang.org/ | Solidity 官方文档 |

#### **PolkaVM (PVM)**

| 资源 | 链接 | 说明 |
|------|------|------|
| PolkaVM Spec | https://github.com/koute/polkavm | PolkaVM 规范 |
| WASM Docs | https://webassembly.org/docs/ | WebAssembly 文档 |

---

### 2.2 赛道二文档

#### **Bifrost 官方资源**

| 资源 | 链接 | 说明 |
|------|------|------|
| Bifrost Docs | https://docs.bifrost.finance | 官方文档 |
| Bifrost GitHub | https://github.com/bifrost-finance | 代码仓库 |
| Bifrost Wiki | https://wiki.bifrost.finance | 知识库 |
| API Reference | https://bifrost.subscan.io/ | 区块浏览器 + API |

#### **技术视频教程**

| 教程 | 链接 | 时长 |
|------|------|------|
| Bifrost 入门 | https://youtube.com/@bifrostfinance | 15分钟 |
| SLP 协议讲解 | 同上 | 20分钟 |
| vToken 开发 | 同上 | 30分钟 |

---

## 💻 代码模板库

### 3.1 赛道一: 智能合约模板

#### **ERC-20 Token 模板 (Solidity)**

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

import "@openzeppelin/contracts/token/ERC20/ERC20.sol";
import "@openzeppelin/contracts/access/Ownable.sol";

contract MyToken is ERC20, Ownable {
    constructor(
        string memory name,
        string memory symbol,
        uint256 initialSupply
    ) ERC20(name, symbol) {
        _mint(msg.sender, initialSupply * 10**decimals());
    }

    function mint(address to, uint256 amount) public onlyOwner {
        _mint(to, amount);
    }

    function burn(uint256 amount) public {
        _burn(msg.sender, amount);
    }
}
```

#### **Foundry 测试模板**

```solidity
// test/MyToken.t.sol
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

import "forge-std/Test.sol";
import "../src/MyToken.sol";

contract MyTokenTest is Test {
    MyToken public token;
    address public owner = address(1);
    address public user1 = address(2);

    function setUp() public {
        vm.prank(owner);
        token = new MyToken("MyToken", "MTK", 1000000);
    }

    function testInitialSupply() public {
        assertEq(token.totalSupply(), 1000000 * 10**18);
        assertEq(token.balanceOf(owner), 1000000 * 10**18);
    }

    function testTransfer() public {
        vm.prank(owner);
        token.transfer(user1, 100 * 10**18);

        assertEq(token.balanceOf(user1), 100 * 10**18);
    }

    function testMint() public {
        vm.prank(owner);
        token.mint(user1, 500 * 10**18);

        assertEq(token.balanceOf(user1), 500 * 10**18);
    }

    function testFailMintUnauthorized() public {
        vm.prank(user1);
        token.mint(user1, 100 * 10**18); // 应该失败
    }
}
```

#### **Hardhat 部署脚本**

```typescript
// scripts/deploy.ts
import { ethers } from "hardhat";

async function main() {
  console.log("部署 MyToken...");

  const MyToken = await ethers.getContractFactory("MyToken");
  const token = await MyToken.deploy(
    "MyToken",
    "MTK",
    1000000 // 初始供应量
  );

  await token.deployed();

  console.log(`MyToken 已部署到: ${token.address}`);

  // 验证合约 (可选)
  console.log("等待区块确认...");
  await token.deployTransaction.wait(5);

  console.log("验证合约...");
  await hre.run("verify:verify", {
    address: token.address,
    constructorArguments: ["MyToken", "MTK", 1000000],
  });
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
```

---

### 3.2 赛道二: 前端模板

#### **React + TypeScript 项目结构**

```
bifrost-frontend/
├── src/
│   ├── components/
│   │   ├── Dashboard/
│   │   │   ├── index.tsx
│   │   │   ├── AssetCard.tsx
│   │   │   └── YieldChart.tsx
│   │   ├── Staking/
│   │   │   ├── MintForm.tsx
│   │   │   └── RedeemForm.tsx
│   │   └── Common/
│   │       ├── Header.tsx
│   │       ├── Footer.tsx
│   │       └── WalletConnect.tsx
│   ├── hooks/
│   │   ├── useBifrost.ts
│   │   ├── useVTokenBalance.ts
│   │   ├── useMintVToken.ts
│   │   └── useExchangeRate.ts
│   ├── services/
│   │   ├── bifrostService.ts
│   │   └── priceService.ts
│   ├── utils/
│   │   ├── format.ts
│   │   └── constants.ts
│   ├── types/
│   │   └── index.ts
│   └── App.tsx
├── public/
├── package.json
└── tsconfig.json
```

#### **钱包连接组件模板**

```typescript
// components/WalletConnect.tsx
import { useState, useEffect } from 'react';
import { web3Accounts, web3Enable } from '@polkadot/extension-dapp';
import type { InjectedAccountWithMeta } from '@polkadot/extension-inject/types';

export function WalletConnect() {
  const [accounts, setAccounts] = useState<InjectedAccountWithMeta[]>([]);
  const [selectedAccount, setSelectedAccount] = useState<string | null>(null);

  useEffect(() => {
    connectWallet();
  }, []);

  async function connectWallet() {
    // 请求 Polkadot.js 扩展权限
    const extensions = await web3Enable('My Bifrost App');

    if (extensions.length === 0) {
      alert('请先安装 Polkadot.js 扩展');
      return;
    }

    // 获取所有账户
    const allAccounts = await web3Accounts();
    setAccounts(allAccounts);

    if (allAccounts.length > 0) {
      setSelectedAccount(allAccounts[0].address);
    }
  }

  return (
    <div className="wallet-connect">
      {!selectedAccount ? (
        <button onClick={connectWallet} className="btn-primary">
          连接钱包
        </button>
      ) : (
        <div className="account-info">
          <select
            value={selectedAccount}
            onChange={(e) => setSelectedAccount(e.target.value)}
            className="account-select"
          >
            {accounts.map((account) => (
              <option key={account.address} value={account.address}>
                {account.meta.name} ({account.address.slice(0, 6)}...
                {account.address.slice(-4)})
              </option>
            ))}
          </select>
          <span className="connected-badge">✓ 已连接</span>
        </div>
      )}
    </div>
  );
}
```

#### **质押表单组件**

```typescript
// components/Staking/MintForm.tsx
import { useState } from 'react';
import { useMintVToken } from '@/hooks/useMintVToken';
import { useVTokenBalance } from '@/hooks/useVTokenBalance';

export function MintForm({ account }: { account: string }) {
  const [amount, setAmount] = useState('');
  const { mint, minting, error } = useMintVToken();
  const { balance } = useVTokenBalance(account, 'DOT');

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();

    if (!amount || parseFloat(amount) <= 0) {
      alert('请输入有效金额');
      return;
    }

    try {
      // 转换为 Planck (1 DOT = 10^10 Planck)
      const amountInPlanck = (parseFloat(amount) * 1e10).toString();

      await mint(account, amountInPlanck);

      alert('质押成功!');
      setAmount('');
    } catch (err) {
      console.error('质押失败:', err);
      alert(`质押失败: ${err.message}`);
    }
  }

  return (
    <form onSubmit={handleSubmit} className="mint-form">
      <h3>铸造 vDOT</h3>

      <div className="form-group">
        <label>质押金额</label>
        <div className="input-group">
          <input
            type="number"
            value={amount}
            onChange={(e) => setAmount(e.target.value)}
            placeholder="0.00"
            min="0"
            step="0.01"
            disabled={minting}
          />
          <span className="input-suffix">DOT</span>
        </div>
        <div className="balance-info">
          可用余额: {(parseFloat(balance) / 1e10).toFixed(4)} DOT
        </div>
      </div>

      <div className="info-box">
        <p>
          您将收到: <strong>{amount || '0'} vDOT</strong>
        </p>
        <p className="text-sm">
          当前汇率: 1 vDOT = 1.0125 DOT
        </p>
      </div>

      {error && <div className="error-message">{error.message}</div>}

      <button
        type="submit"
        disabled={minting || !amount}
        className="btn-primary"
      >
        {minting ? '铸造中...' : '铸造 vDOT'}
      </button>
    </form>
  );
}
```

---

## 🌐 测试网资源

### 4.1 测试网配置

#### **Moonbase Alpha (Moonbeam 测试网)**

```typescript
// config/networks.ts
export const MOONBASE_ALPHA = {
  name: 'Moonbase Alpha',
  rpc: 'https://rpc.api.moonbase.moonbeam.network',
  chainId: 1287,
  currency: {
    name: 'DEV',
    symbol: 'DEV',
    decimals: 18,
  },
  explorer: 'https://moonbase.moonscan.io/',
  faucet: 'https://faucet.moonbeam.network/',
};
```

#### **Bifrost Testnet**

```typescript
export const BIFROST_TESTNET = {
  name: 'Bifrost Testnet',
  rpc: 'wss://bifrost-polkadot-testnet.liebi.com/ws',
  chainId: null, // Substrate chain
  explorer: 'https://bifrost-testnet.subscan.io/',
  tokens: {
    DOT: '0x0000000000000000000000000000000000000000',
    vDOT: '0x0001000000000000000000000000000000000000',
  },
};
```

---

### 4.2 测试代币水龙头

| 网络 | 水龙头链接 | 每次领取 | 冷却时间 |
|------|-----------|---------|---------|
| Moonbase Alpha | https://faucet.moonbeam.network/ | 1 DEV | 24小时 |
| Polkadot Testnet | https://faucet.polkadot.io/ | 10 DOT | 24小时 |
| Bifrost Testnet | Discord | 联系团队 | - |

---

## 🎨 UI/UX 设计资源

### 5.1 设计系统

#### **Tailwind CSS 配置**

```javascript
// tailwind.config.js
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#f0f9ff',
          100: '#e0f2fe',
          500: '#0ea5e9',
          600: '#0284c7',
          700: '#0369a1',
        },
        secondary: {
          500: '#8b5cf6',
          600: '#7c3aed',
        },
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
        mono: ['Fira Code', 'monospace'],
      },
    },
  },
  plugins: [require('@tailwindcss/forms')],
};
```

#### **设计代币**

```css
/* styles/globals.css */
:root {
  /* 间距 */
  --spacing-xs: 0.25rem;
  --spacing-sm: 0.5rem;
  --spacing-md: 1rem;
  --spacing-lg: 1.5rem;
  --spacing-xl: 2rem;

  /* 圆角 */
  --radius-sm: 0.25rem;
  --radius-md: 0.5rem;
  --radius-lg: 1rem;

  /* 阴影 */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);

  /* 过渡 */
  --transition-fast: 150ms ease;
  --transition-base: 300ms ease;
  --transition-slow: 500ms ease;
}
```

---

### 5.2 UI 组件库

**推荐库**:

| 库 | 特点 | 链接 |
|-----|------|------|
| **Chakra UI** | 易用、可访问性好 | https://chakra-ui.com/ |
| **shadcn/ui** | 可定制、Tailwind | https://ui.shadcn.com/ |
| **Ant Design** | 企业级、组件丰富 | https://ant.design/ |
| **Material-UI** | Material Design | https://mui.com/ |

**图表库**:

| 库 | 特点 | 使用场景 |
|-----|------|----------|
| **Recharts** | React 原生、简单 | 基础图表 |
| **Chart.js** | 轻量、灵活 | 各种图表 |
| **D3.js** | 强大、可定制 | 复杂可视化 |
| **ApexCharts** | 现代、动画丰富 | 交互式图表 |

---

### 5.3 图标资源

```bash
# 安装图标库
npm install lucide-react  # 推荐
npm install react-icons
npm install @heroicons/react
```

**使用示例**:

```tsx
import { Wallet, TrendingUp, DollarSign } from 'lucide-react';

function Dashboard() {
  return (
    <div className="dashboard">
      <div className="stat-card">
        <Wallet className="icon" />
        <h3>总资产</h3>
        <p>$125,450</p>
      </div>

      <div className="stat-card">
        <TrendingUp className="icon" />
        <h3>总收益</h3>
        <p>+$1,234</p>
      </div>
    </div>
  );
}
```

---

## 🚀 部署与 DevOps

### 6.1 智能合约部署

#### **Foundry 部署命令**

```bash
# 编译合约
forge build

# 运行测试
forge test -vvv

# 部署到 Moonbase Alpha
forge create --rpc-url $MOONBASE_RPC \
  --private-key $PRIVATE_KEY \
  --constructor-args "MyToken" "MTK" 1000000 \
  src/MyToken.sol:MyToken

# 验证合约
forge verify-contract \
  --chain-id 1287 \
  --compiler-version v0.8.19 \
  <CONTRACT_ADDRESS> \
  src/MyToken.sol:MyToken
```

#### **环境变量配置 (.env)**

```bash
# .env
MOONBASE_RPC=https://rpc.api.moonbase.moonbeam.network
PRIVATE_KEY=0x...
ETHERSCAN_API_KEY=...
```

---

### 6.2 前端部署

#### **Vercel 部署 (推荐)**

```bash
# 安装 Vercel CLI
npm install -g vercel

# 登录
vercel login

# 部署
vercel

# 生产环境部署
vercel --prod
```

#### **Netlify 部署**

```bash
# 安装 Netlify CLI
npm install -g netlify-cli

# 登录
netlify login

# 部署
netlify deploy --prod
```

#### **Docker 部署**

```dockerfile
# Dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
```

```bash
# 构建镜像
docker build -t bifrost-app .

# 运行容器
docker run -p 3000:3000 bifrost-app
```

---

## 🔧 常见问题解决

### 7.1 智能合约常见错误

#### **错误 1: Gas Estimation Failed**

```
原因: 交易会 revert,或者 Gas 限制太低

解决方案:
1. 检查合约逻辑是否有 require 失败
2. 手动设置 Gas Limit
```

```typescript
// 手动设置 Gas
const tx = await contract.transfer(to, amount, {
  gasLimit: 500000, // 手动指定
});
```

#### **错误 2: Nonce Too Low**

```
原因: Nonce 不匹配 (通常是重复交易)

解决方案:
1. 等待之前的交易完成
2. 清除待处理交易
```

```typescript
// 获取正确的 nonce
const nonce = await provider.getTransactionCount(account, 'pending');

const tx = await contract.transfer(to, amount, { nonce });
```

---

### 7.2 Polkadot.js 常见问题

#### **错误 1: Extension Not Found**

```typescript
// 解决方案: 检测扩展是否安装
async function checkExtension() {
  const extensions = await web3Enable('My App');

  if (extensions.length === 0) {
    alert('请安装 Polkadot.js 扩展: https://polkadot.js.org/extension/');
    return false;
  }

  return true;
}
```

#### **错误 2: Invalid Signature**

```typescript
// 解决方案: 确保使用正确的 signer
import { web3FromSource } from '@polkadot/extension-dapp';

async function signTransaction(account, tx) {
  // 获取账户的 signer
  const injector = await web3FromSource(account.meta.source);

  // 使用正确的 signer 签名
  const hash = await tx.signAndSend(account.address, { signer: injector.signer });

  return hash;
}
```

---

### 7.3 性能优化

#### **优化 1: 减少不必要的链上查询**

```typescript
// ❌ 不好: 每次都查询
function BadComponent({ account }) {
  const [balance, setBalance] = useState(0);

  useEffect(() => {
    // 每次渲染都查询
    fetchBalance(account).then(setBalance);
  });

  return <div>{balance}</div>;
}

// ✅ 好: 使用依赖数组 + 缓存
function GoodComponent({ account }) {
  const [balance, setBalance] = useState(0);

  useEffect(() => {
    let isSubscribed = true;

    fetchBalance(account).then((bal) => {
      if (isSubscribed) setBalance(bal);
    });

    return () => {
      isSubscribed = false;
    };
  }, [account]); // 仅当 account 变化时查询

  return <div>{balance}</div>;
}
```

#### **优化 2: 批量查询**

```typescript
// ❌ 不好: 多次单独查询
async function fetchAllBalances(accounts) {
  const balances = [];
  for (const account of accounts) {
    const balance = await api.query.system.account(account);
    balances.push(balance);
  }
  return balances;
}

// ✅ 好: 批量查询
async function fetchAllBalancesBatch(accounts) {
  const balances = await api.query.system.account.multi(accounts);
  return balances;
}
```

---

## 📊 评审标准详解

### 赛道一评分细则

| 维度 | 权重 | 评分要点 | 如何拿高分 |
|------|------|---------|-----------|
| **技术实现** | 30% | • 代码质量<br>• 架构设计<br>• 功能完整性 | • 代码整洁、注释清晰<br>• 模块化设计<br>• 核心功能完全实现 |
| **创新性** | 25% | • 独创性<br>• 实用价值<br>• 差异化 | • 解决真实痛点<br>• 提供独特价值<br>• 有明确的应用场景 |
| **Polkadot 整合** | 25% | • XCM 使用<br>• REVM/PVM 利用<br>• 跨链能力 | • 深度利用 Polkadot 特性<br>• 展示跨链优势<br>• 原生集成 |
| **开发者体验** | 10% | • 文档完整性<br>• 易用性<br>• 工具链 | • README 详细<br>• 提供示例代码<br>• 一键运行 |
| **项目潜力** | 10% | • 可持续性<br>• 市场需求<br>• 扩展性 | • 清晰的商业模式<br>• 有市场验证<br>• 可扩展架构 |

---

### 赛道二评分细则

| 维度 | 权重 | 评分要点 | 如何拿高分 |
|------|------|---------|-----------|
| **用户体验** | 35% | • 界面美观<br>• 交互流畅<br>• 易用性 | • 精美的 UI 设计<br>• 流畅的动画<br>• 直观的操作流程 |
| **技术实现** | 30% | • 功能完整<br>• 代码质量<br>• 性能 | • 核心功能完整<br>• 代码规范<br>• 快速响应 |
| **创新性** | 20% | • 设计新颖<br>• 独特价值<br>• 差异化 | • 创新的交互方式<br>• 独特的功能<br>• 超越竞品 |
| **Bifrost 整合** | 15% | • vToken 支持<br>• API 集成<br>• 多链支持 | • 支持多种 vToken<br>• 深度 API 集成<br>• 跨链功能 |

---

## 🏆 获奖项目特征

### 高分项目共同点

1. **清晰的价值主张**
   - 明确解决什么问题
   - 为谁解决 (目标用户)
   - 如何解决 (技术方案)

2. **完整的 Demo**
   - 可运行的演示
   - 核心功能完整
   - 稳定性高

3. **专业的文档**
   - README 详细
   - 架构图清晰
   - 使用说明完备

4. **精美的演示**
   - 视频演示流畅
   - PPT 设计专业
   - 讲解逻辑清晰

---

## 📈 时间管理建议

### 3天冲刺计划

**Day 1 (30%时间)**:
- ✅ 搭建基础架构
- ✅ 实现核心功能 MVP
- ✅ 完成基础测试

**Day 2 (50%时间)**:
- ✅ 完善功能
- ✅ 优化性能
- ✅ UI/UX 改进

**Day 3 (20%时间)**:
- ✅ Bug 修复
- ✅ 文档完善
- ✅ 部署上线
- ✅ 准备演示

---

## 🎓 学习资源推荐

### 视频教程

| 教程 | 时长 | 难度 | 链接 |
|------|------|------|------|
| Polkadot 基础 | 2小时 | 入门 | YouTube |
| Substrate 开发 | 5小时 | 中级 | Substrate.io |
| Solidity 精通 | 10小时 | 中高级 | Udemy |
| React 全栈 | 8小时 | 中级 | FreeCodeCamp |

### 书籍推荐

1. **《Mastering Blockchain》** - 区块链全面指南
2. **《Polkadot: Vision for a Heterogeneous Multi-Chain Framework》** - Polkadot 白皮书
3. **《Solidity Programming Essentials》** - Solidity 编程
4. **《Full Stack React》** - React 开发

---

## 🤝 社区支持

### 技术支持渠道

| 平台 | 用途 | 响应时间 |
|------|------|---------|
| Discord | 实时聊天、技术讨论 | < 1小时 |
| GitHub Issues | Bug 报告、功能请求 | < 24小时 |
| Stack Overflow | 技术问答 | < 12小时 |
| Twitter | 公告、快速问答 | < 2小时 |

### 官方社区

- **Polkadot Discord**: https://discord.gg/polkadot
- **Bifrost Discord**: https://discord.gg/bifrost
- **Substrate Builders Program**: https://substrate.io/builders-program/

---

**祝你黑客松顺利! 🚀**

如有任何问题,随时在社区提问!
